[build]
builder = "nixpacks"
buildCommand = "cd backend && python manage.py collectstatic --noinput"

[deploy]
startCommand = "cd backend && python manage.py migrate && gunicorn team_manager.wsgi:application --bind 0.0.0.0:$PORT"
restartPolicyType = "ON_FAILURE"
